import React from 'react';
import './TrackList.css';

class TrackList extends React.Component {

    render() {
        return(
          <div class="TrackList">
            <p>we will add a map method that renders a set of Track components</p>
          </div>
        );
    }
}

export default TrackList;
